{{> header}}
<main class="login_page min-h-[80vh] flex items-center justify-center">
    <section class="container bg-white max-w-sm my-sm rounded-xl p-4 flex flex-col gap-4">
        <h1 class="text-center font-bold text-xl text-indigo-900 mt-4">{{title}}</h1>
        <div class="contain flex items-center justify-center"><img src="/images/favicon.ico" class="w-[5rem]" alt="logo"></div>
        {{> form}}
    </section>
</main>
{{> footer}}